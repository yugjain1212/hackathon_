{
  "name": "terraalert-functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "serve": "npm run build && firebase emulators:start --only functions,hosting,firestore,storage",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint ."
  },
  "engines": {
    "node": "20"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.7.7",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "geolib": "^3.3.4",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.11.30",
    "eslint": "^9.11.1",
    "typescript": "^5.6.3"
  }
}
